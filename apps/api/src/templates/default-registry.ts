import { SheetRegistry } from '../engines/excel-template-engine/types';

export const defaultRegistry: SheetRegistry = {
  fieldMappings: { sheetName: 'TDD', startRow: 6, styleRow: 6, startColumn: 1 }
};
